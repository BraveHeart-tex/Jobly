'use client';
import { Icon } from '@chakra-ui/react';
import { AiOutlineHome } from 'react-icons/ai';

const HomeIcon = () => {
  return <Icon as={AiOutlineHome} w={7} h={7} />;
};

export default HomeIcon;
