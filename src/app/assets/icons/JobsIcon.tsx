'use client';
import { Icon } from '@chakra-ui/react';
import { MdQueryStats } from 'react-icons/md';

const JobsIcon = () => {
  return <Icon as={MdQueryStats} w={7} h={7} />;
};

export default JobsIcon;
